# Tasks 046 - Landing Brand Isotype

- [x] 1. Reuse a shared 48 pixel landing logo in web and dashboard navigation, mobile navigation, and footer; complete quality review and applicable verification.
- [x] 2. Extract the dark background into identical transparent PNGs for both apps, switch the shared source, and verify alpha, visual fidelity, and quality review.
- [x] 3. Enlarge web and dashboard header logos to 80 pixels while preserving footer size, mobile fit, hero clearance, and anchor visibility; complete quality review and applicable verification.
- [x] 4. Enlarge both footer logos to 80 pixels, match the web brand link height, and complete quality review and verification.
- [x] 5. Use the shared transparent logo for web and dashboard favicon and Apple touch icon metadata; complete review and verification.
- [x] 6. Create a symbol-only square favicon with dark background for both apps, update icon metadata, and complete visual asset review and verification.

Task 6 verification: identical 512 pixel opaque PNGs contain only the original lime symbol on #121311. Asset previews at 16, 32, and 48 pixels are recognizable. Shared UI type checking, lint, touched-file formatting, and git diff --check passed. Read-only quality review passed with no findings. No new tests were added because this is visual asset and metadata polish of low test importance. Browser verification remains unavailable.

Task 5 verification: lint, touched-file formatting, and git diff --check passed. Read-only quality review passed with no findings: both root routes reference the exported shared PNG source for icon and apple-touch-icon and both public assets exist. No new tests were added because this is a cosmetic metadata update of low test importance. Global type checks were not repeated after the previously recorded compiler failures. Browser verification remains unavailable.

Task 4 verification: shared UI type checking, lint, touched-file formatting, and git diff --check passed. Read-only quality review passed with no findings: all header and footer logos now measure 80 pixels and the web footer link encloses the image. New tests were omitted because this is a cosmetic sizing change of low test importance. Browser verification remains unavailable.

Task 3 verification: shared UI type checking, lint, touched-file format check, 29 existing dashboard landing tests, and git diff --check passed. Read-only quality review passed after mobile brand padding and web control gaps were reduced to accommodate signed-in navigation at 320 pixels while preserving 44 pixel controls. Header logos are 80 pixels, bars 96 pixels, footer logos 48 pixels, and section anchor margins 128 pixels. No new tests were added because this is a visual sizing adjustment of low test importance. Browser verification remains unavailable. Earlier global type and formatting limitations remain recorded below.

Task 2: the user explicitly authorized local pixel processing after three built-in extraction attempts introduced noisy remnants and were rejected. The original image was extracted to a 1059 by 1008 RGBA PNG with transparent background and internal negative spaces. Both apps have byte-identical PNGs and use the shared PNG source at the existing 48 pixel size. Original JPGs were preserved. Shared UI type checking, lint, touched-file formatting, and git diff --check passed. Read-only quality review passed after inspecting transparency, asset equality, and original contours. No new tests were added because test importance is low. Browser verification remains unavailable; the original pale wordmark has low contrast on light backgrounds.

Verification: lint, touched-file formatting, four existing dashboard header tests, and git diff --check passed. Shared UI type checking passed. Global type checking crashed in admin and scoped client checking crashed in web with TypeScript's "Debug Failure. No error for last overload signature". Global formatting reported 1385 files; only task files were formatted. Quality review passed after web brand link heights were corrected to 48 pixels. Browser verification was unavailable because no browser backend was connected. Keep the feature active until visual verification is available.
