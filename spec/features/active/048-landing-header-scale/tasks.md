# Tasks 048 - Landing Header Scale

- [x] T1: Reduce the shared landing header logo and both landing header containers
      to the compact scale, then update the public web shell clearance.
- [ ] T2: Verify the affected frontend type-checks and inspect the final diff for
      formatting and scope.
- [x] T3: Use the shared landing logo in the `web` and `dashboard` authentication
      shells so login, registration, and password-reset flows share the same
      brand mark.
- [x] T4: Switch the shared generic logo source to the favicon, provision that
      asset in all three apps, and remove the obsolete `logo.png` files.
